//! `reasoner` — wraps the `reasonable` OWL 2 RL reasoner.
//!
//! Chapter 3 fills in `closure()` which loads schema + data Turtle
//! files, runs forward-chaining, and returns the inferred triples.

pub fn placeholder() {}
